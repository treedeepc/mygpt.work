# mygpt.work 订单追踪：换浏览器也能查询

[English](./ORDER-TRACKING.md)

付款之后最让人焦虑的不是等待几分钟，而是不知道订单进行到哪一步。mygpt.work 把付款状态和充值状态分别记录，并提供两种查询方式。

## 原下单浏览器

下单时的浏览器可直接打开[订单查询](https://mygpt.work/orders)，查看该浏览器创建的订单、付款进度和充值进度。

## 换浏览器或清除 Cookie

1. 输入下单时使用的邮箱。
2. 接收并填写 6 位一次性验证码。
3. 验证码 10 分钟有效。
4. 验证成功后获得 24 小时只读查询权限。

为了降低邮箱枚举风险，系统在完成邮箱验证前不会直接透露该邮箱是否存在订单。发送验证码有前后端 60 秒冷却和服务端频率限制。

## 权限边界

- 邮箱验证可以查看订单，不能发起付款。
- 邮箱验证不能补充账号授权。
- 付款或补充授权仍需使用原下单浏览器。
- 订单号不是身份凭证，不能单独用于读取订单。

这种设计既解决了换设备后的查询问题，也保留了敏感操作的访问边界。
