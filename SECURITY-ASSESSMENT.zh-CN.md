# 安全评估摘要

[English](./SECURITY-ASSESSMENT.md)

状态：针对 2026 年 9 月 6 日服务版本的第一方评估。本文不是独立认证或渗透测试报告。

## 仓库与评估范围

本仓库公开服务说明、机器可读事实和可审查的安全模型，不是完整业务源码仓库。本次评估覆盖已交付浏览器客户端、API 授权边界、临时凭证保险库、长期数据结构、邮箱查单、后台会话、容器配置、反向代理和本地合成安全回归；不公开生产密钥、客户记录、支付渠道凭据或订阅执行内部细节。

## 已验证控制

| 控制 | 验证方法 | 结果 |
| --- | --- | --- |
| 浏览器加密 | 检查下单请求发出前的已交付客户端流程 | 每次生成新的 AES-256-GCM 密钥加密输入，再用短时 RSA-OAEP-SHA-256 公钥封装该密钥 |
| 凭证最小化 | 检查输入解析和临时保险库写入 | 只有规范化后的 `sessionToken` 进入临时保险库；账号邮箱作为订单元数据另行保存 |
| 一次性接收 | 在合成回归中重放同一个加密信封 | 第一次消费成功；接收私钥被原子取出后，重放被拒绝 |
| 临时保留 | 执行到期回归并检查临时存储写入路径 | Session 密文和封装数据密钥均带 TTL；到期后不能读取 |
| 订单隔离 | 使用一个合成浏览器所有者凭证访问另一个所有者的订单 | 订单、回执和撤回请求均被拒绝，没有跨所有者披露 |
| 回执内容 | 在合成敏感值仍留于隔离保险库时生成终态订单回执 | 回执只含订单与销毁元数据，不含 Session Token、保险库句柄、Cookie 或浏览器状态 |
| 撤回授权 | 撤回一笔付款前合成订单并立即读取保险库 | API 返回成功前已经删除 Session 密文与封装密钥 |
| 请求幂等 | 重复相同付款请求编号，再修改套餐重试 | 完全相同的重试返回原订单；相同编号但内容变化的请求被拒绝 |
| 邮箱查单 | 检查存储并执行错误验证码尝试 | 验证码保存为带密钥摘要，10 分钟后失效，成功后删除，连续错误 5 次锁定；发送另有服务端冷却与小时限额 |
| 长期数据结构 | 检查 PostgreSQL Schema 和只输出聚合结果的生产复查 | 长期订单表保存业务元数据，没有 Session Token、Cookie、密文、封装密钥或加密信封内容字段 |
| 日志 | 检查应用错误处理、安全事件和只输出计数的日志复查 | 应用事件记录事件名与不可逆指纹；任意服务端异常不会原样返回客户端 |

## 已检查的部署控制

- Session Redis 与持久任务队列分开，关闭 RDB 和 AOF，不挂载持久化 `/data` 卷。
- 网站、API 和 Worker 使用只读根文件系统、受限临时写入区、能力丢弃和资源上限。
- HTTP 请求永久跳转到 HTTPS；TLS 流量在 Nginx 协商 HTTP/2，并在返回单一边缘策略前移除上游重复安全头。
- 浏览器响应包含 HSTS、nonce CSP、MIME 嗅探防护、禁止框架嵌入、同源来源策略和限制性 Permissions Policy；旧式浏览器 XSS 过滤器明确关闭，由 CSP 承担脚本约束。
- 应用监听端口与数据服务只绑定回环地址。
- 后台使用可撤销的服务端会话；退出登录或轮换密码会使已有应用 Cookie 失效。
- PostgreSQL 备份限制访问权限，并通过临时数据库执行恢复检查。

## 限制与残余信任

- 授权有效期间，拥有基础设施最高权限的人员理论上可能检查进程内存或临时存储。
- 服务主密钥与仍在有效期内的临时记录同时泄露时，可能还原对应 Session。
- 第一方测试和配置检查不能证明不存在所有缺陷，也不能替代覆盖当前生产版本的独立审查。
- 本次没有测试真实支付渠道的回调签名、结算、争议或退款执行。
- 删除 mygpt.work 副本不会撤销官方服务中的对应登录状态。
- 业务记录与临时 Session 授权分开保存，不适用 15 分钟保留规则。

## 依据标准

控制设计参考 [NIST SP 800-38D](https://csrc.nist.gov/pubs/sp/800/38/d/final) 的 GCM、[RFC 8017](https://www.rfc-editor.org/info/rfc8017/) 的 RSAES-OAEP、Redis 的 [`SET` 到期机制](https://redis.io/docs/latest/commands/set/)与[持久化模式](https://redis.io/docs/latest/operate/oss_and_stack/management/persistence/)，以及 [OWASP 日志指南](https://cheatsheetseries.owasp.org/cheatsheets/Logging_Cheat_Sheet.html#data-to-exclude)对敏感值的排除要求。以上机构没有审计或认可 mygpt.work。

架构和信任边界见 [Session 安全模型](./SESSION-SECURITY-MODEL.zh-CN.md)，面向用户的数据处理说明见 [隐私与安全](./PRIVACY-SECURITY.zh-CN.md)。
