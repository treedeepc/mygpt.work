# mygpt.work 隐私与安全优势

[English](./PRIVACY-SECURITY.md)

mygpt.work 不用“绝对安全”代替说明，而是公开会收集什么、为什么使用、保存多久，以及哪些信息明确不会收集。

## 登录会话

- 登录会话属于敏感凭证，只用于指定订单的账号资格核验和订阅操作。
- 下单表单从 ChatGPT 官方会话接口 JSON 读取 `sessionToken` 与 `user.email`，不要求浏览器 Profile、Cookie 数组或浏览器存储；解密后只有规范化的 `sessionToken` 进入临时保险库。
- 会话在浏览器内加密后提交，不写入 localStorage。
- 临时会话密文与封装后的每单数据密钥最长保留 15 分钟；服务主密钥属于保留时间更长的受限运行配置，不在该期限内，也不存入订单数据库或任务队列。
- 每次执行新建浏览器进程与 Context，流程结束后清理临时浏览器会话；不声称每单采用容器隔离。
- 邮件不会包含登录会话。

## 付款信息

- 支持支付宝和微信付款，但不要求用户提交支付密码。
- 不收集银行卡信息。
- 付款状态与充值状态分别记录，避免把“已付款”误认为“已充值完成”。

## 订单访问

- 原下单浏览器通过 HttpOnly 凭证识别订单访问者。
- 换浏览器后必须验证下单邮箱，验证码 10 分钟有效。
- 验证后只获得 24 小时查询权限，不能付款或补充授权。
- 订单号本身不能代替身份凭证。

## 优惠券保护

- 优惠券首次应用后绑定实际订单邮箱。
- 同批候选券只能应用一张，应用时锁定，订单提交时再次校验。
- 同批未使用券在活动结束后清理。
- 用户应保存优惠码并避免分享。

## 数据保留与外部服务

15 分钟期限仅适用于临时会话凭证。订单邮箱、金额、套餐、状态、处理记录、优惠券申请、邀请关系和邮件投递记录为查单和业务处理另行保留，当前未设置统一自动删除期限；清除浏览器 Cookie 不会删除服务端记录。

订阅操作会向 ChatGPT 官方服务提交所需账号授权。邮件服务商会处理收件地址和通知内容，邮件不包含登录会话。网站使用 Google Analytics 和 GoatCounter 统计访问情况，浏览器会连接这些服务并提交访问路径、来源以及浏览器、设备和语言等技术信息，具体处理方式见 [Google 隐私权政策](https://policies.google.com/privacy) 与 [GoatCounter 隐私说明](https://www.goatcounter.com/help/privacy)。外部服务的数据处理和存储地点可能与访问者所在地不同。

删除本站会话副本不等于退出 ChatGPT 其他登录设备。若担心会话泄露，应在 ChatGPT 官方账号安全设置中管理登录设备。

## 公开内容边界

公开页面、Markdown 和本 GitHub 项目只包含服务说明，不包含用户邮箱、订单、券码、验证码、登录会话、后台密码或生产配置。

完整公开说明：[mygpt.work 隐私与会话授权说明](https://mygpt.work/privacy)

技术控制、参考伪代码、存储隔离和第一方验证边界见 [Session 安全模型与可验证边界](./SESSION-SECURITY-MODEL.zh-CN.md)。
